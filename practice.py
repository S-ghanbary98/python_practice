# Declare function that takes in a username argument as as string. then returns the name.

#def username(name):
#   return name

# Declare a list with number 1 - 5. Iterate through the list and display the list

ls = [1,2, 3,4,5]

for i in ls:
    print(i)